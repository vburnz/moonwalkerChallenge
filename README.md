# Instructions for Opening in Storybook 

run `npm install` 
run `yarn run storybook`
navigate to localhost:9009

# Instructions for Opening as React App

run `npm install` 
run `yarn start`
navigate to localhost:3000

